machine:
  node:
    version: 8.9.4


test:
  override:
    - "true"

deployment:
  production:
    branch: qa
    codedeploy:
      we-r-next-generation-web:
        application_root: /
        region: us-east-1
        revision_location:
          revision_type: S3
          s3_location:
            bucket: wrngtest
            key_pattern: we-r-next-generation-web-{BRANCH}-{SHORT_COMMIT}
